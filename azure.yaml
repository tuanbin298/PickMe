# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/refs/heads/main/schemas/v1.0/azure.yaml.json

name: pickme
metadata:
  template: pickme-app@0.0.1-beta

services:
  backend:
    project: ./PickMeApplication
    language: java
    host: appservice
    
  frontend:
    project: ./pickme_fe_web
    language: js
    host: staticwebapp