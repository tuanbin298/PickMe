# Database Configuration - Neon PostgreSQL

spring.datasource.url=${NEON_DATABASE_URL:jdbc:postgresql://ep-solitary-thunder-ad72gnev-pooler.c-2.us-east-1.aws.neon.tech/pickmeapplication?sslmode=require&channel_binding=require&options=-c%20TimeZone=Asia/Ho_Chi_Minh}
spring.datasource.username=${NEON_DB_USERNAME:neondb_owner}
spring.datasource.password=${NEON_DB_PASSWORD:npg_7QKchdN0pnJX}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.spatial.dialect.postgis.PostgisPG95Dialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.database-platform=org.hibernate.spatial.dialect.postgis.PostgisPG95Dialect

# Neon specific configurations
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000

# JWT Configuration
jwt.secret=${JWT_SECRET:mySecretKey12345678901234567890123456789012345678901234567890}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Server Configuration
server.address=${SERVER_ADDRESS:0.0.0.0}
server.port=${SERVER_PORT:8080}

# Timezone Configuration
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Ho_Chi_Minh
app.timezone=Asia/Ho_Chi_Minh

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${MAIL_PASSWORD:your-16-digit-app-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Application Email Settings
app.email.from=${MAIL_FROM:your-email@gmail.com}
app.email.name=PickMe Application
app.base-url=${APP_BASE_URL:http://localhost:8080}

# SePay Configuration
sepay.bank.name=${SEPAY_BANK_NAME:VPBank}
sepay.account.number=${SEPAY_ACCOUNT_NUMBER:0868767029}
sepay.account.holder=${SEPAY_ACCOUNT_HOLDER:PICK ME APPLICATION}
sepay.qr.base.url=${SEPAY_QR_BASE_URL:https://qr.sepay.vn}
sepay.webhook.secret=${SEPAY_WEBHOOK_SECRET:}
sepay.webhook.url=${SEPAY_WEBHOOK_URL:https://shops-miss-joshua-warm.trycloudflare.com/api/payments/sepay/webhook}

# Payment Configuration
payment.expiry.minutes=${PAYMENT_EXPIRY_MINUTES:15}
payment.auto.expire=${PAYMENT_AUTO_EXPIRE:true}

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.health.db.enabled=true

# Logging
logging.level.org.springframework.security=${LOGGING_LEVEL:INFO}
logging.level.org.hibernate.SQL=DEBUG