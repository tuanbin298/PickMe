# Neon Database Configuration
# Cấu hình database Neon PostgreSQL (cloud)
NEON_DATABASE_URL=jdbc:postgresql://ep-solitary-thunder-ad72gnev-pooler.c-2.us-east-1.aws.neon.tech/pickmeapplication?sslmode=require&channel_binding=require
NEON_DB_USERNAME=neondb_owner
NEON_DB_PASSWORD=npg_7QKchdN0pnJX

# Legacy Database Configuration (for local PostgreSQL - backup option)
# Cấu hình database PostgreSQL local (tùy chọn backup)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=pickmeapplication
DB_USERNAME=myuser
DB_PASSWORD=your_database_password

# JWT Configuration
# JWT secret key (ít nhất 32 ký tự) và thời gian hết hạn (milliseconds)
JWT_SECRET=your_jwt_secret_key_at_least_32_characters_long
JWT_EXPIRATION=86400000

# Email Configuration (Gmail SMTP)
# Cấu hình email để gửi thông báo
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-16-digit-app-password
MAIL_FROM=your-email@gmail.com

# Application Configuration
# URL base của ứng dụng
APP_BASE_URL=http://localhost:8080
APP_ENV=development

# Server Configuration
# Địa chỉ IP để bind server (0.0.0.0 = tất cả IP, localhost = chỉ local)
SERVER_ADDRESS=0.0.0.0
# Port chạy ứng dụng
SERVER_PORT=8080

# SePay Configuration
# Cấu hình thanh toán SePay
SEPAY_BANK_NAME=MBBank
SEPAY_ACCOUNT_NUMBER=0903252427
SEPAY_ACCOUNT_HOLDER=PICK ME APPLICATION
SEPAY_QR_BASE_URL=https://qr.sepay.vn
# Webhook secret để xác thực (tùy chọn)
SEPAY_WEBHOOK_SECRET=your_sepay_webhook_secret_key
# Webhook URL cho SePay gọi (sẽ dùng Cloudflare Tunnel khi test local)
SEPAY_WEBHOOK_URL=https://shops-miss-joshua-warm.trycloudflare.com/api/payments/sepay/webhook

# Cloudflare Tunnel Configuration (thay thế ngrok)
# URL công khai của Cloudflare Tunnel để truy cập từ bên ngoài
CLOUDFLARED_URL=https://shops-miss-joshua-warm.trycloudflare.com

# Payment Configuration
# Thời gian hết hạn cho payment pending (minutes)
PAYMENT_EXPIRY_MINUTES=15
# Tự động expire pending payments (true/false)
PAYMENT_AUTO_EXPIRE=true

# Logging Level
# Mức độ log: TRACE, DEBUG, INFO, WARN, ERROR
LOGGING_LEVEL=INFO